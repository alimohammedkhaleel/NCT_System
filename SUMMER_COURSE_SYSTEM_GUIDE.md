# 📚 دليل نظام الدراسة الصيفية - Summer Course System Guide

## 🎯 نظرة عامة

نظام الدراسة الصيفية مُطبق بالكامل في النظام ويعمل تلقائياً عند نقل الطلاب.

---

## 📋 قواعد الدراسة الصيفية

### السنة الأولى والثالثة (Year 1 & 3)
- ✅ **حتى 3 مواد راسب** → الطالب ينتقل إلى `summer_course`
- ❌ **أكثر من 3 مواد راسب** → الطالب يعيد السنة (`repeat_year`)

### السنة الثانية والرابعة (Year 2 & 4) - سنتا التخرج
- ⚠️ **يجب النجاح في جميع المواد**
- ❌ **أي رسوب (حتى مادة واحدة)** → الطالب ينتقل إلى `summer_course`
- 🎓 لا يوجد خيار "إعادة السنة" في سنوات التخرج

---

## 🔄 دورة حياة الطالب في الدراسة الصيفية

```
┌─────────────────────────────────────────────────────────────┐
│  1. الطالب يرسب في مادة/مواد                                │
│     ↓                                                        │
│  2. النظام يطبق القواعد تلقائياً                            │
│     ↓                                                        │
│  3. academic_status = 'summer_course'                       │
│     ↓                                                        │
│  4. الدكتور يدخل درجات الدراسة الصيفية                      │
│     ↓                                                        │
│  5. Admin ينشر النتائج                                      │
│     ↓                                                        │
│  6. Admin يستخدم "النقل الجماعي للسنة الجديدة"              │
│     ↓                                                        │
│  7. النظام يتحقق من نجاح الطالب في الصيفي                  │
│     ↓                                                        │
│  8a. نجح → ينتقل للسنة الجديدة (current_year++)            │
│  8b. رسب → يبقى في نفس السنة                                │
└─────────────────────────────────────────────────────────────┘
```

---

## 🗄️ قاعدة البيانات

### حقل `academic_status` في جدول `students`

```sql
ENUM('active', 'graduated', 'suspended', 'transferred', 'dropped', 'summer_course')
```

### Migration المطلوبة

```bash
# إضافة summer_course إلى ENUM (تم بالفعل ✅)
node server/add-summer-course-status.js

# إضافة عمود current_semester (جديد ⚡)
node server/add-current-semester-column.js
```

---

## 💻 كيفية إدخال درجات الدراسة الصيفية

### 1. Professor Dashboard

الدكتور يستطيع إدخال الدرجات للطلاب في حالة `summer_course` **بنفس الطريقة** التي يدخل بها الدرجات العادية:

#### الخطوات:
1. الدكتور يدخل إلى **Professor Dashboard**
2. يختار **التخصص** → **السنة الدراسية** → **الترم**
3. يختار **المادة** التي فيها طلاب راسبون
4. النظام يعرض **جميع الطلاب المسجلين** في المادة (بما فيهم طلاب `summer_course`)
5. الدكتور يدخل الدرجات الجديدة
6. يضغط **"حفظ الدرجات"**

#### ملاحظات مهمة:
- ✅ النظام **لا يفرق** بين الطالب العادي والطالب في الدراسة الصيفية عند إدخال الدرجات
- ✅ الدكتور يستطيع **تعديل الدرجات** القديمة أو إدخال درجات جديدة
- ✅ النظام يحسب **النسبة المئوية** و **التقدير** تلقائياً

### 2. Grade Model

```javascript
// جدول grades يحتوي على:
{
  student_id: INTEGER,
  course_id: INTEGER,
  semester_id: INTEGER,
  academic_year_id: INTEGER,
  assignment1_grade: DECIMAL,
  assignment2_grade: DECIMAL,
  final_exam_score: DECIMAL,
  total_percentage: DECIMAL,
  letter_grade: STRING,
  status: ENUM('draft', 'pending_admin_approval', 'approved', 'rejected'),
  is_published: BOOLEAN
}
```

#### كيف يعمل النظام:
1. عندما الدكتور يدخل درجات جديدة للطالب الراسب، النظام:
   - يبحث عن Grade record موجود للطالب في نفس المادة
   - إذا موجود → **يعدل** الدرجات القديمة (UPDATE)
   - إذا غير موجود → **ينشئ** record جديد (INSERT)

2. الدرجات تمر بنفس workflow:
   ```
   draft → pending_admin_approval → approved → published
   ```

---

## 🎓 النقل الجماعي للسنة الجديدة

### Backend Logic في `promoteToNextYear`

```javascript
// server/controllers/adminController.js

const studentWhere = {
  academic_status: { [Op.in]: ['active', 'summer_course'] }, // ✅ يشمل طلاب الصيفي
  current_year: yearNumber
};

// للطالب في summer_course:
if (failedCourses.length === 0) {
  // نجح في الصيفي → ينتقل للسنة الجديدة
  await student.update({ 
    current_year: yearNumber + 1, 
    academic_status: 'active',
    current_semester: 1 // يبدأ من الترم الأول
  });
} else {
  // رسب في الصيفي → يبقى في نفس السنة
  await student.update({ 
    academic_status: 'active' // يرجع active لكن نفس السنة
  });
}
```

---

## 🖥️ واجهة المستخدم

### Admin Dashboard - Students Management

#### الجدول الجديد:

| كود الطالب | الاسم | الرقم القومي | التخصص | السنة | **الترم الحالي** | الفرع | الحالة | الإجراءات |
|------------|------|--------------|---------|-------|------------------|-------|--------|-----------|
| 12345678 | أحمد محمد | 30012... | ICT | 2 | **الترم الأول** | برمجيات | نشط | تعديل |
| 87654321 | سارة علي | 30112... | ICT | 1 | **الترم الثاني** | شبكات | **دراسة صيفية** | تعديل |

#### التغييرات:
- ✅ **إضافة عمود "الترم الحالي"** - يعرض الترم الحالي للطالب
- ❌ **حذف أزرار النقل الفردي** - لا فائدة منها
- ✅ **الإبقاء على زر "تعديل" فقط** - لتعديل بيانات الطالب
- ✅ **النقل الجماعي** - الطريقة الوحيدة للنقل

### ملاحظة النقل الجماعي المحدثة:

```
⚙️ النظام يتحقق تلقائياً من الشروط:
• السنة الثانية والرابعة (سنتا التخرج) — يجب النجاح في جميع المواد → أي رسوب = دراسة صيفية ☀️
• السنة الأولى والثالثة — حتى 3 مواد راسب → دراسة صيفية ☀️، أكثر من 3 → إعادة السنة 🔁
• الطلاب الناجحون في الدراسة الصيفية ينتقلون للسنة الجديدة مع باقي الطلاب ✅
• النظام يطبق القواعد تلقائياً على كل طالب حسب حالته
```

---

## 🔍 سيناريوهات الاستخدام

### سيناريو 1: طالب سنة أولى رسب في مادتين

```
1. نهاية الترم الثاني → Admin يستخدم "نقل للسنة الجديدة"
2. النظام يكتشف: رسب في 2 مواد (≤3)
3. academic_status = 'summer_course'
4. الطالب يبقى في السنة الأولى
5. الدكتور يدخل درجات الصيفي
6. Admin ينشر النتائج
7. Admin يستخدم "نقل للسنة الجديدة" مرة أخرى
8. النظام يتحقق:
   - نجح في الصيفي → current_year = 2, academic_status = 'active'
   - رسب في الصيفي → يبقى current_year = 1, academic_status = 'active'
```

### سيناريو 2: طالب سنة ثانية (تخرج) رسب في مادة واحدة

```
1. نهاية الترم الثاني → Admin يستخدم "نقل للسنة الجديدة"
2. النظام يكتشف: سنة تخرج + رسب في مادة واحدة
3. academic_status = 'summer_course' (حتى لو مادة واحدة!)
4. الطالب يبقى في السنة الثانية
5. الدكتور يدخل درجات الصيفي
6. Admin ينشر النتائج
7. Admin يستخدم "نقل للسنة الجديدة"
8. النظام يتحقق:
   - نجح في الصيفي → current_year = 3, academic_status = 'active'
   - رسب في الصيفي → يبقى current_year = 2, academic_status = 'active'
```

### سيناريو 3: طالب سنة رابعة (تخرج نهائي) رسب في مادة

```
1. نهاية الترم الثاني → Admin يستخدم "تخريج" (bulk)
2. النظام يكتشف: سنة 4 + رسب في مادة
3. academic_status = 'summer_course'
4. الطالب لا يتخرج
5. الدكتور يدخل درجات الصيفي
6. Admin ينشر النتائج
7. Admin يستخدم "تخريج" مرة أخرى
8. النظام يتحقق:
   - نجح في الصيفي → academic_status = 'graduated'
   - رسب في الصيفي → يبقى في السنة الرابعة
```

---

## ✅ ما تم تطبيقه

### Backend ✅
- [x] حقل `summer_course` في `academic_status` ENUM
- [x] Migration script: `add-summer-course-status.js`
- [x] حقل `current_semester` في Student model
- [x] Migration script: `add-current-semester-column.js`
- [x] قواعد الترقية في `promoteToNextSemester`
- [x] قواعد الترقية في `promoteToNextYear`
- [x] قواعد الترقية في `studentController.promoteStudent`
- [x] النظام يشمل طلاب `summer_course` في النقل الجماعي

### Frontend ✅
- [x] عرض حالة "دراسة صيفية" في جدول الطلاب
- [x] Badge خاص بحالة `summer_course`
- [x] عمود "الترم الحالي" في الجدول
- [x] حذف أزرار النقل الفردي
- [x] تحديث ملاحظة النقل الجماعي
- [x] نتائج النقل الجماعي تعرض طلاب الصيفي

### Professor Dashboard ✅
- [x] الدكتور يستطيع إدخال/تعديل درجات جميع الطلاب
- [x] النظام لا يفرق بين الطالب العادي وطالب الصيفي
- [x] Grade workflow يعمل بشكل طبيعي

---

## 🚀 خطوات التشغيل

### 1. تشغيل Migrations

```bash
# إضافة summer_course status
node server/add-summer-course-status.js

# إضافة current_semester column
node server/add-current-semester-column.js
```

### 2. إعادة تشغيل السيرفر

```bash
cd server
npm start
```

### 3. اختبار النظام

1. أدخل درجات لطالب (بعضها راسب)
2. استخدم "النقل الجماعي للسنة الجديدة"
3. تحقق من تغيير `academic_status` إلى `summer_course`
4. أدخل درجات جديدة للطالب (ناجحة)
5. استخدم "النقل الجماعي" مرة أخرى
6. تحقق من انتقال الطالب للسنة الجديدة

---

## 📝 ملاحظات مهمة

1. **الدرجات القديمة لا تُحذف** - يتم تعديلها فقط
2. **النظام يحتفظ بتاريخ الدرجات** - كل تعديل له timestamp
3. **Admin يجب أن ينشر النتائج** قبل النقل الجماعي
4. **الطلاب في `summer_course` يظهرون في Professor Dashboard** بشكل طبيعي
5. **لا يوجد "ترم صيفي" منفصل** - الدرجات تُدخل في نفس الترم

---

## 🎯 الخلاصة

✅ **نظام الدراسة الصيفية مُطبق بالكامل**
✅ **الدكتور يستطيع إدخال الدرجات بشكل طبيعي**
✅ **النظام يطبق القواعد تلقائياً**
✅ **الواجهة نظيفة وبسيطة**

---

**تاريخ التحديث**: 2026-04-22
**الحالة**: ✅ جاهز للاستخدام
