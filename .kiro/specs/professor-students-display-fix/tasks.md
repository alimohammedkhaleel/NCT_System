# خطة التنفيذ

- [x] 1. كتابة اختبار استكشافي لشرط الخلل
  - **Property 1: Bug Condition** - فلترة الطلاب بدون مراعاة الترم
  - **مهم جداً**: يجب كتابة هذا الاختبار قبل تنفيذ الإصلاح
  - **حرج**: هذا الاختبار يجب أن يفشل على الكود غير المُصلح - الفشل يؤكد وجود الخلل
  - **لا تحاول إصلاح الاختبار أو الكود عند فشله**
  - **ملاحظة**: هذا الاختبار يحتوي على السلوك المتوقع - سيتحقق من الإصلاح عندما ينجح بعد التنفيذ
  - **الهدف**: إظهار أمثلة مضادة توضح وجود الخلل
  - **منهج PBT محدد النطاق**: للأخطاء الحتمية، حدد نطاق الخاصية للحالات الفاشلة الملموسة لضمان إمكانية التكرار
  - اختبار أن getStudentsByCourse تُرجع طلاب من ترمات مختلفة عند اختيار مادة في ترم محدد
  - يجب أن يتحقق الاختبار من:
    - عند اختيار مادة في الترم الأول (semester_id = 1)، يتم إرجاع طلاب من الترم الثاني أيضاً (خطأ)
    - عند اختيار مادة في الترم الثاني (semester_id = 2)، يتم إرجاع طلاب من الترم الأول أيضاً (خطأ)
  - تشغيل الاختبار على الكود غير المُصلح
  - **النتيجة المتوقعة**: فشل الاختبار (هذا صحيح - يثبت وجود الخلل)
  - توثيق الأمثلة المضادة المكتشفة لفهم السبب الجذري
  - إتمام المهمة عند كتابة الاختبار وتشغيله وتوثيق الفشل
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 1.6_

- [-] 2. كتابة اختبارات الحفاظ على السلوك (قبل تنفيذ الإصلاح)
  - **Property 2: Preservation** - الحفاظ على وظائف إدارة الدرجات
  - **مهم**: اتباع منهجية المراقبة أولاً
  - مراقبة السلوك على الكود غير المُصلح للمدخلات غير المتعلقة بالخلل
  - كتابة اختبارات قائمة على الخصائص تلتقط أنماط السلوك المُلاحظ من متطلبات الحفاظ
  - الاختبار القائم على الخصائص يولد العديد من حالات الاختبار لضمانات أقوى
  - يجب أن تتحقق الاختبارات من:
    - حفظ الدرجات يستمر في حساب المجموع والنسبة المئوية والتقدير بشكل صحيح
    - إرسال الدرجات للمراجعة يستمر في تحديث الحالة من draft إلى submitted
    - جلب إعدادات المادة يستمر في إرجاع القيم الصحيحة أو الافتراضية
    - التحقق من صلاحيات الأستاذ يستمر في منع الوصول غير المصرح به
    - تصفية المواد بناءً على التخصص والسنة الدراسية تستمر في العمل
  - تشغيل الاختبارات على الكود غير المُصلح
  - **النتيجة المتوقعة**: نجاح الاختبارات (يؤكد السلوك الأساسي المطلوب الحفاظ عليه)
  - إتمام المهمة عند كتابة الاختبارات وتشغيلها ونجاحها على الكود غير المُصلح
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7, 3.8, 3.9, 3.10_

- [ ] 3. إصلاح مشكلة عرض الطلاب في لوحة الأستاذ

  - [ ] 3.1 إضافة حقل current_semester إلى نموذج Student
    - فتح ملف `server/models/Student.js`
    - إضافة حقل `current_semester` من نوع INTEGER
    - تعيين `allowNull: true` في البداية للسماح بالبيانات الموجودة
    - إضافة foreign key reference يشير إلى جدول `semesters`
    - إضافة index على الحقل الجديد لتحسين الأداء
    - إضافة تعليق توضيحي: 'الترم الحالي للطالب'
    - _Bug_Condition: isBugCondition(input) حيث course.semester_id IS NOT NULL AND students لا تُفلتر بناءً على semester_id_
    - _Expected_Behavior: جدول students يحتوي على حقل current_semester يشير إلى semester_id من جدول semesters_
    - _Preservation: الحفاظ على جميع الحقول والعلاقات الموجودة في نموذج Student_
    - _Requirements: 1.4, 2.3, 2.6_

  - [ ] 3.2 تحديث دالة getStudentsByCourse لتفلتر بناءً على الترم
    - فتح ملف `server/controllers/gradeController.js`
    - تحديد موقع دالة `getStudentsByCourse` (حوالي السطر 1644)
    - تحديث شرط WHERE في استعلام Student.findAll
    - إضافة `current_semester: course.semester_id` إلى شروط الفلترة
    - التأكد من أن الفلترة تتضمن: specialty_id, current_year, current_semester
    - _Bug_Condition: isBugCondition(input) حيث getStudentsByCourse تُرجع طلاب من ترمات مختلفة_
    - _Expected_Behavior: getStudentsByCourse تُرجع فقط الطلاب الذين ينتمون لنفس specialty_id و current_year و current_semester_
    - _Preservation: الحفاظ على جميع includes والعلاقات الموجودة في الاستعلام_
    - _Requirements: 1.1, 1.2, 1.5, 1.6, 2.1, 2.2, 2.4, 2.5_

  - [ ] 3.3 تحديث دالة createStudent لتعيين الترم الافتراضي
    - فتح ملف `server/controllers/studentController.js`
    - تحديد موقع دالة `createStudent`
    - إضافة `current_semester: 1` إلى بيانات Student.create
    - التأكد من تعيين الفصل الدراسي الأول كقيمة افتراضية
    - _Bug_Condition: عند إنشاء طالب جديد، لا يتم تعيين current_semester_
    - _Expected_Behavior: عند إنشاء طالب جديد، يتم تعيين current_semester = 1 بشكل افتراضي_
    - _Preservation: الحفاظ على جميع الحقول الأخرى وعملية الإنشاء_
    - _Requirements: 2.7_

  - [ ] 3.4 تحديث دالة promoteStudent لتحديث الترم عند الترقية
    - فتح ملف `server/controllers/studentController.js`
    - تحديد موقع دالة `promoteStudent`
    - إضافة منطق لتحديث current_semester عند promotion_type === 'semester'
    - حساب الترم التالي: إذا كان الترم الحالي 1، ينتقل إلى 2، وإذا كان 2، ينتقل إلى 1
    - عند الانتقال من الترم 2 إلى 1، زيادة current_year إذا لم يكن في السنة الرابعة
    - تحديث رسالة النجاح لتوضيح الترم الجديد
    - _Bug_Condition: عند ترقية الطالب، لا يتم تحديث current_semester_
    - _Expected_Behavior: عند ترقية الطالب من فصل دراسي لآخر، يتم تحديث current_semester بشكل صحيح_
    - _Preservation: الحفاظ على منطق الترقية للسنة الدراسية والتخريج_
    - _Requirements: 2.8_

  - [ ] 3.5 إنشاء migration script لإضافة الحقل الجديد
    - إنشاء ملف migration جديد في مجلد migrations
    - إضافة عمود `current_semester` إلى جدول `students`
    - تعيين قيمة افتراضية (1) لجميع الطلاب الموجودين
    - إضافة foreign key constraint يشير إلى جدول `semesters`
    - إضافة index على العمود الجديد
    - اختبار تشغيل المigration على قاعدة بيانات تطوير
    - _Bug_Condition: جدول students لا يحتوي على حقل current_semester_
    - _Expected_Behavior: جدول students يحتوي على حقل current_semester مع قيم صحيحة_
    - _Preservation: الحفاظ على جميع البيانات الموجودة في جدول students_
    - _Requirements: 1.4, 2.3, 2.6_

  - [ ] 3.6 التحقق من نجاح اختبار استكشاف شرط الخلل
    - **Property 1: Expected Behavior** - فلترة الطلاب بناءً على الترم
    - **مهم**: إعادة تشغيل نفس الاختبار من المهمة 1 - لا تكتب اختبار جديد
    - الاختبار من المهمة 1 يحتوي على السلوك المتوقع
    - عندما ينجح هذا الاختبار، يؤكد أن السلوك المتوقع تم تحقيقه
    - تشغيل اختبار استكشاف شرط الخلل من الخطوة 1
    - **النتيجة المتوقعة**: نجاح الاختبار (يؤكد إصلاح الخلل)
    - التحقق من أن:
      - عند اختيار مادة في الترم الأول، يتم إرجاع طلاب الترم الأول فقط
      - عند اختيار مادة في الترم الثاني، يتم إرجاع طلاب الترم الثاني فقط
      - لا يتم إرجاع طلاب من ترمات مختلفة
    - _Requirements: 2.1, 2.2, 2.4, 2.5_

  - [ ] 3.7 التحقق من استمرار نجاح اختبارات الحفاظ على السلوك
    - **Property 2: Preservation** - الحفاظ على وظائف إدارة الدرجات
    - **مهم**: إعادة تشغيل نفس الاختبارات من المهمة 2 - لا تكتب اختبارات جديدة
    - تشغيل اختبارات الحفاظ على السلوك من الخطوة 2
    - **النتيجة المتوقعة**: نجاح الاختبارات (يؤكد عدم حدوث انحدار)
    - التأكد من نجاح جميع الاختبارات بعد الإصلاح (عدم حدوث انحدار)
    - التحقق من أن جميع وظائف إدارة الدرجات تعمل بشكل صحيح
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7, 3.8, 3.9, 3.10_

- [ ] 4. نقطة تفتيش - التأكد من نجاح جميع الاختبارات
  - التأكد من نجاح جميع الاختبارات، اسأل المستخدم إذا ظهرت أسئلة
  - التحقق من أن:
    - اختبار استكشاف شرط الخلل ينجح (الخلل تم إصلاحه)
    - اختبارات الحفاظ على السلوك تنجح (لا انحدار)
    - جميع الاختبارات الموجودة تنجح
  - مراجعة الكود للتأكد من جودة التنفيذ
  - توثيق أي ملاحظات أو توصيات للمستخدم
