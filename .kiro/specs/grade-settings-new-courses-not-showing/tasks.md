# خطة التنفيذ

- [x] 1. كتابة اختبار استكشافي لشرط الخلل
  - **Property 1: Bug Condition** - عرض الكورسات الجديدة مع القيم الافتراضية
  - **مهم جداً**: هذا الاختبار يجب أن يفشل على الكود غير المُصلح - الفشل يؤكد وجود الخلل
  - **لا تحاول إصلاح الاختبار أو الكود عندما يفشل**
  - **ملاحظة**: هذا الاختبار يحدد السلوك المتوقع - سيتحقق من الإصلاح عندما ينجح بعد التنفيذ
  - **الهدف**: إظهار أمثلة مضادة توضح وجود الخلل
  - **منهج PBT محدد النطاق**: للأخطاء الحتمية، حدد نطاق الخاصية للحالات الفاشلة المحددة لضمان إمكانية التكرار
  - اختبار أن الكورسات الموجودة في جدول Courses ولكن ليس لها سجل في CourseGradeConfig لا تظهر في نتائج getAllConfigs
  - يجب أن يتحقق الاختبار من شرط الخلل: `isBugCondition(course)` حيث course موجود في Courses ولكن course.id غير موجود في CourseGradeConfig
  - تشغيل الاختبار على الكود غير المُصلح
  - **النتيجة المتوقعة**: الاختبار يفشل (هذا صحيح - يثبت وجود الخلل)
  - توثيق الأمثلة المضادة الموجودة لفهم السبب الجذري
  - وضع علامة على المهمة كمكتملة عندما يتم كتابة الاختبار وتشغيله وتوثيق الفشل
  - _Requirements: 2.1, 2.2, 2.3_

- [x] 2. كتابة اختبارات الحفاظ على السلوك (قبل تنفيذ الإصلاح)
  - **Property 2: Preservation** - الحفاظ على عرض الكورسات ذات الـ Config المخصص
  - **مهم**: اتبع منهج المراقبة أولاً
  - مراقبة: الكورسات التي لها config في CourseGradeConfig تظهر بقيمها المخصصة على الكود غير المُصلح
  - مراقبة: الفلاتر (specialty_id, academic_year_id, semester_id) تعمل بشكل صحيح على الكود غير المُصلح
  - مراقبة: هيكل البيانات المُرجع يحتوي على جميع الحقول المطلوبة (id, course_id, ass1_percentage, إلخ)
  - كتابة اختبارات قائمة على الخصائص: لجميع الكورسات التي لها config (حيث NOT isBugCondition)، النتيجة تطابق القيم المخصصة من CourseGradeConfig
  - الاختبار القائم على الخصائص يولد العديد من حالات الاختبار لضمانات أقوى
  - تشغيل الاختبارات على الكود غير المُصلح
  - **النتيجة المتوقعة**: الاختبارات تنجح (هذا يؤكد السلوك الأساسي الذي يجب الحفاظ عليه)
  - وضع علامة على المهمة كمكتملة عندما يتم كتابة الاختبارات وتشغيلها ونجاحها على الكود غير المُصلح
  - _Requirements: 3.1, 3.2, 3.4, 3.6_

- [ ] 3. إصلاح دالة getAllConfigs في courseGradeConfigController.js

  - [x] 3.1 تنفيذ الإصلاح
    - تغيير نقطة البداية للاستعلام من `CourseGradeConfig.findAll()` إلى `Course.findAll()`
    - إضافة LEFT JOIN إلى CourseGradeConfig باستخدام `include` مع `required: false`
    - نقل شروط التصفية (specialty_id, academic_year_id, semester_id) إلى `where` الرئيسي على جدول Course
    - إضافة منطق القيم الافتراضية: فحص إذا كان CourseGradeConfig موجود أو null
    - إذا كان null، استخدام القيم الافتراضية (ass1: 15%, ass2: 15%, final: 70%, إلخ)
    - إذا كان موجود، استخدام القيم المخصصة (السلوك الحالي)
    - الحفاظ على هيكل البيانات المُرجع (نفس الحقول والأسماء)
    - تحديث الترتيب ليعمل مع البنية الجديدة
    - _Bug_Condition: isBugCondition(course) حيث course موجود في Courses ولكن course.id غير موجود في CourseGradeConfig_
    - _Expected_Behavior: لجميع الكورسات التي تحقق isBugCondition، يجب أن تظهر في نتائج getAllConfigs مع القيم الافتراضية المحددة_
    - _Preservation: الكورسات التي لها config مخصص يجب أن تستمر في عرض قيمها المخصصة، الفلاتر يجب أن تستمر في العمل، هيكل البيانات يجب أن يبقى كما هو_
    - _Requirements: 2.1, 2.2, 2.3, 3.1, 3.2, 3.4, 3.6_

  - [x] 3.2 التحقق من نجاح اختبار استكشاف شرط الخلل
    - **Property 1: Expected Behavior** - عرض الكورسات الجديدة مع القيم الافتراضية
    - **مهم**: إعادة تشغيل نفس الاختبار من المهمة 1 - لا تكتب اختبار جديد
    - الاختبار من المهمة 1 يحدد السلوك المتوقع
    - عندما ينجح هذا الاختبار، يؤكد أن السلوك المتوقع تم تحقيقه
    - تشغيل اختبار استكشاف شرط الخلل من الخطوة 1
    - **النتيجة المتوقعة**: الاختبار ينجح (يؤكد أن الخلل تم إصلاحه)
    - _Requirements: 2.1, 2.2, 2.3_

  - [x] 3.3 التحقق من استمرار نجاح اختبارات الحفاظ على السلوك
    - **Property 2: Preservation** - الحفاظ على عرض الكورسات ذات الـ Config المخصص
    - **مهم**: إعادة تشغيل نفس الاختبارات من المهمة 2 - لا تكتب اختبارات جديدة
    - تشغيل اختبارات الحفاظ على السلوك من الخطوة 2
    - **النتيجة المتوقعة**: الاختبارات تنجح (تؤكد عدم وجود انحدارات)
    - التأكد من أن جميع الاختبارات تنجح بعد الإصلاح (لا توجد انحدارات)
    - _Requirements: 3.1, 3.2, 3.4, 3.6_

- [x] 4. نقطة تفتيش - التأكد من نجاح جميع الاختبارات
  - التأكد من نجاح جميع الاختبارات، اسأل المستخدم إذا ظهرت أسئلة.
