# Contributing to NCTU ERP System

🎓 مرحباً بك في مشروع نظام إدارة جامعة القاهرة الجديدة التكنولوجية! نحن نقدر مساهمتك في تطوير هذا النظام.

## 🚀 كيفية المساهمة

### 1. إعداد البيئة المحلية

```bash
# استنساخ المشروع
git clone https://github.com/your-username/nctu-erp.git
cd nctu-erp

# تثبيت التبعيات
npm run install:all

# إعداد قاعدة البيانات
npm run setup:db

# تشغيل المشروع
npm run start
```

### 2. إنشاء فرع جديد

```bash
# إنشاء فرع للميزة الجديدة
git checkout -b feature/amazing-feature

# أو إنشاء فرع لإصلاح خطأ
git checkout -b fix/bug-fix
```

### 3. إجراء التغييرات

- اتبع معايير الترميز المحددة
- أضف اختبارات للتغييرات الجديدة
- تأكد من عمل التطبيق بشكل صحيح
- حدث التوثيق إذا لزم الأمر

### 4. إرسال التغييرات

```bash
# إضافة التغييرات
git add .

# إنشاء commit مع رسالة واضحة
git commit -m "Add: amazing new feature"

# رفع التغييرات
git push origin feature/amazing-feature
```

### 5. إنشاء Pull Request

1. اذهب إلى GitHub وأنشئ Pull Request
2. وصف التغييرات بوضوح
3. ربط أي issues ذات صلة
4. انتظار المراجعة

## 📋 معايير الترميز

### JavaScript/React
- استخدم ES6+ features
- استخدم async/await بدلاً من promises
- استخدم const/let بدلاً من var
- اتبع نمط camelCase للمتغيرات والدوال
- اتبع نمط PascalCase للمكونات

### CSS/SCSS
- استخدم BEM methodology
- استخدم CSS custom properties للألوان والمتغيرات
- اجعل الكود responsive
- استخدم Flexbox/Grid للتخطيط

### Backend (Node.js)
- استخدم async/await
- تحقق من صحة البيانات المدخلة
- استخدم middleware للمصادقة
- أضف error handling شامل

## 🧪 الاختبار

```bash
# تشغيل جميع الاختبارات
npm test

# تشغيل اختبارات الواجهة الأمامية
npm run test:frontend

# تشغيل اختبارات الخادم
npm run test:backend
```

## 📚 التوثيق

- حدث README.md عند إضافة ميزات جديدة
- أضف JSDoc comments للدوال الجديدة
- وثق API endpoints في ملف منفصل

## 🐛 الإبلاغ عن الأخطاء

عند الإبلاغ عن خطأ، يرجى تضمين:

1. **وصف الخطأ**: ما الذي حدث؟
2. **خطوات التكرار**: كيف يمكن تكرار الخطأ؟
3. **السلوك المتوقع**: ما الذي كان يجب أن يحدث؟
4. **السلوك الفعلي**: ما الذي حدث فعلاً؟
5. **البيئة**: نظام التشغيل، المتصفح، إصدار Node.js
6. **لقطات شاشة**: إذا أمكن

## 💡 اقتراحات الميزات

نرحب باقتراحات الميزات الجديدة! يرجى:

1. التحقق من أن الميزة غير موجودة بالفعل
2. وصف الميزة بوضوح
3. شرح الفائدة منها
4. مناقشة كيفية تنفيذها

## 📞 التواصل

- **Issues**: للأخطاء والميزات الجديدة
- **Discussions**: للأسئلة والمناقشات العامة
- **Email**: info@nctu.edu.eg

## 🎯 إرشادات محددة للمشروع

### قيود الأساتذة
- تأكد من أن كل أستاذ يمكنه فقط الوصول لدرجات مادته
- تحقق من صحة الصلاحيات في كل من الواجهة الأمامية والخلفية

### الأمان
- لا تقم بتخزين كلمات المرور كنص واضح
- استخدم JWT للمصادقة
- تحقق من صحة البيانات المدخلة

### الأداء
- استخدم lazy loading للمكونات
- قم بتحسين الصور
- استخدم caching عند الإمكان

### إمكانية الوصول
- تأكد من دعم قارئات الشاشة
- استخدم ألوان مناسبة لضعاف البصر
- اجعل التطبيق يعمل بالكيبورد

## 🙏 شكراً لمساهمتك!

نقدر وقتك وجهدك في تطوير هذا النظام. كل مساهمة تساعد في تحسين تجربة الطلاب والأساتذة في جامعة القاهرة الجديدة التكنولوجية.