# دليل اختبار شامل - إضافة دكتور وتعيين المواد

## 🎯 الهدف
اختبار كامل لإضافة دكتور جديد وتعيين المواد له بالشكل الأمثل

---

## 📋 المتطلبات الأساسية

### 1. التأكد من وجود البيانات الأساسية:
```sql
-- التحقق من وجود التخصصات
SELECT * FROM specialties WHERE is_active = 1;
-- يجب أن يظهر 6 تخصصات: MCT, AUT, ICT, PRO, OIL, REN

-- التحقق من وجود السنوات الدراسية
SELECT * FROM academic_years;
-- يجب أن يظهر 24 سنة (6 تخصصات × 4 سنوات)

-- التحقق من وجود المواد
SELECT * FROM courses;
-- يجب أن يكون هناك مواد مرتبطة بالتخصصات والسنوات
```

### 2. التأكد من تشغيل الـ Server:
```bash
# في terminal
cd server
node server.js

# يجب أن يظهر:
# ✅ Database connection established successfully.
# ✅ Database tables synced successfully.
# 🚀 Server is running on port 5000
```

### 3. التأكد من تشغيل الـ Frontend:
```bash
# في terminal آخر
cd client/frontend
npm run dev

# يجب أن يظهر:
# ➜  Local:   http://localhost:5173/
```

---

## 🧪 خطوات الاختبار الشاملة

### المرحلة 1: تسجيل الدخول كـ Admin

1. افتح المتصفح على `http://localhost:5173`
2. اذهب إلى صفحة Login
3. سجل دخول بحساب Admin:
   - **Username**: `admin`
   - **Password**: `admin123`
4. ✅ **النتيجة المتوقعة**: تسجيل دخول ناجح وتوجيه إلى Admin Dashboard

---

### المرحلة 2: الذهاب إلى صفحة Professors

1. من Admin Dashboard، اضغط على "Professors Management"
2. أو اذهب مباشرة إلى: `http://localhost:5173/admin/professors`
3. ✅ **النتيجة المتوقعة**: 
   - عرض قائمة الدكاترة الحاليين (إن وجدوا)
   - زر "Add New Professor" ظاهر في الأعلى

---

### المرحلة 3: إضافة دكتور جديد

#### 3.1 فتح نموذج الإضافة
1. اضغط على زر "+ Add New Professor"
2. ✅ **النتيجة المتوقعة**: فتح modal بعنوان "Add New Professor"

#### 3.2 ملء البيانات الأساسية
املأ الحقول التالية:

| الحقل | القيمة المقترحة | ملاحظات |
|-------|-----------------|---------|
| **Username** | `dr_ahmed` | للدخول إلى النظام |
| **Password** | `ahmed123` | كلمة مرور قوية |
| **Full Name** | `Dr. Ahmed Hassan` | الاسم الكامل |
| **التخصص** | اختر `تكنولوجيا المعلومات (ICT)` | **مهم جداً!** |
| **Email** | `ahmed@nctu.edu` | بريد إلكتروني صحيح |
| **Phone** | `01012345678` | اختياري |
| **Department** | `Computer Science` | اختياري |
| **Specialization** | `Software Engineering` | اختياري |

#### 3.3 حفظ البيانات
1. اضغط على زر "Create"
2. ✅ **النتيجة المتوقعة**:
   - رسالة نجاح: "تم إنشاء حساب الدكتور بنجاح"
   - إغلاق الـ modal
   - ظهور الدكتور الجديد في القائمة
   - كود الدكتور: `PROF-001` (أو الرقم التالي)

#### 3.4 التحقق من البيانات
تحقق من أن الدكتور ظهر في الجدول مع:
- ✅ كود الدكتور (PROF-XXX)
- ✅ الاسم الكامل
- ✅ البريد الإلكتروني
- ✅ رقم الهاتف
- ✅ القسم
- ✅ عدد المواد = 0 (لم يتم تعيين مواد بعد)

---

### المرحلة 4: تعيين المواد للدكتور

#### 4.1 فتح modal تعيين المواد
1. في صف الدكتور الجديد، اضغط على زر "Assign Courses"
2. ✅ **النتيجة المتوقعة**: 
   - فتح modal بعنوان "تعيين مواد للدكتور: Dr. Ahmed Hassan"
   - ظهور dropdown لاختيار التخصص
   - عرض جميع المواد المتاحة

#### 4.2 فلترة المواد حسب التخصص
1. في dropdown "فلترة حسب التخصص"، اختر `تكنولوجيا المعلومات (ICT)`
2. ✅ **النتيجة المتوقعة**:
   - عرض المواد الخاصة بتخصص ICT فقط
   - ظهور رسالة: "ℹ️ يتم عرض المواد الخاصة بالتخصص المختار فقط"
   - كل مادة تعرض:
     - كود المادة (مثل: CS101)
     - اسم المادة بالعربي
     - التخصص - السنة - عدد الساعات

#### 4.3 اختيار المواد
اختر 3-5 مواد من القائمة:
- ✅ عند الضغط على checkbox، يتغير لون الـ card إلى أزرق فاتح
- ✅ الحدود تصبح أكثر وضوحاً (2px solid #0A2472)

#### 4.4 حفظ التعيينات
1. اضغط على زر "Save Assignments"
2. ✅ **النتيجة المتوقعة**:
   - ظهور "جاري الحفظ..." على الزر
   - رسالة نجاح: "تم تعيين المواد بنجاح"
   - إغلاق الـ modal
   - تحديث عدد المواد في الجدول (يجب أن يظهر العدد الصحيح)

---

### المرحلة 5: التحقق من التعيينات

#### 5.1 إعادة فتح modal التعيين
1. اضغط مرة أخرى على "Assign Courses" لنفس الدكتور
2. ✅ **النتيجة المتوقعة**:
   - المواد المعينة سابقاً تظهر محددة (checked)
   - يمكن إضافة مواد جديدة
   - يمكن إلغاء تحديد مواد موجودة

#### 5.2 تعديل التعيينات
1. أضف مادة جديدة أو احذف مادة موجودة
2. اضغط "Save Assignments"
3. ✅ **النتيجة المتوقعة**: 
   - حفظ التغييرات بنجاح
   - تحديث عدد المواد في الجدول

---

### المرحلة 6: اختبار الحالات الخاصة

#### 6.1 محاولة إضافة دكتور بـ username مكرر
1. اضغط "+ Add New Professor"
2. استخدم نفس الـ username السابق (`dr_ahmed`)
3. املأ باقي البيانات
4. اضغط "Create"
5. ✅ **النتيجة المتوقعة**: 
   - رسالة خطأ: "اسم المستخدم مستخدم بالفعل، يرجى اختيار آخر"
   - عدم إنشاء الحساب

#### 6.2 محاولة إضافة دكتور بـ email مكرر
1. استخدم username جديد لكن نفس الـ email السابق
2. ✅ **النتيجة المتوقعة**: 
   - رسالة خطأ: "البريد الإلكتروني مستخدم بالفعل، يرجى اختيار آخر"

#### 6.3 محاولة حفظ بدون اختيار مواد
1. افتح modal التعيين
2. ألغِ تحديد جميع المواد
3. اضغط "Save Assignments"
4. ✅ **النتيجة المتوقعة**: 
   - رسالة خطأ: "Please select at least one course"

#### 6.4 اختبار الفلترة بتخصص فارغ
1. في modal التعيين، اختر "— جميع التخصصات —"
2. ✅ **النتيجة المتوقعة**: 
   - عرض جميع المواد من كل التخصصات
   - إخفاء رسالة الفلترة

---

### المرحلة 7: اختبار تعديل الدكتور

#### 7.1 فتح نموذج التعديل
1. اضغط على زر "Edit" للدكتور
2. ✅ **النتيجة المتوقعة**: 
   - فتح modal بعنوان "Edit Professor"
   - البيانات الحالية معبأة في الحقول
   - حقول Username و Password غير ظاهرة (للتعديل فقط)

#### 7.2 تعديل البيانات
1. غيّر الاسم إلى `Dr. Ahmed Hassan Updated`
2. غيّر القسم إلى `Information Systems`
3. اضغط "Update"
4. ✅ **النتيجة المتوقعة**: 
   - رسالة نجاح
   - تحديث البيانات في الجدول

---

### المرحلة 8: اختبار الحذف

#### 8.1 حذف دكتور
1. اضغط على زر "Delete" للدكتور
2. ✅ **النتيجة المتوقعة**: 
   - ظهور رسالة تأكيد: "Are you sure you want to delete this professor?"
3. اضغط "OK"
4. ✅ **النتيجة المتوقعة**: 
   - رسالة نجاح: "Professor deleted successfully"
   - اختفاء الدكتور من القائمة

---

## 🔍 فحص قاعدة البيانات

### التحقق من إنشاء الدكتور:
```sql
-- عرض جميع الدكاترة
SELECT 
  p.id, p.professor_code, p.department, p.specialization,
  u.username, u.full_name, u.email, u.role
FROM professors p
JOIN users u ON p.user_id = u.id
WHERE u.role = 'professor';
```

### التحقق من تعيين المواد:
```sql
-- عرض المواد المعينة للدكتور
SELECT 
  pc.id, pc.professor_id, pc.course_id,
  c.course_code, c.course_name, c.arabic_name,
  s.name as specialty_name,
  ay.year_number
FROM professor_courses pc
JOIN courses c ON pc.course_id = c.id
JOIN specialties s ON c.specialty_id = s.id
JOIN academic_years ay ON c.academic_year_id = ay.id
WHERE pc.professor_id = 1; -- غيّر الرقم حسب ID الدكتور
```

---

## ✅ قائمة التحقق النهائية

### وظائف أساسية:
- [ ] إضافة دكتور جديد بنجاح
- [ ] ظهور الدكتور في القائمة
- [ ] فتح modal تعيين المواد
- [ ] فلترة المواد حسب التخصص
- [ ] اختيار مواد متعددة
- [ ] حفظ التعيينات بنجاح
- [ ] تحديث عدد المواد في الجدول
- [ ] إعادة فتح modal وعرض المواد المحددة
- [ ] تعديل بيانات الدكتور
- [ ] حذف الدكتور

### التحقق من الأخطاء:
- [ ] رفض username مكرر
- [ ] رفض email مكرر
- [ ] رفض حفظ بدون مواد
- [ ] عرض رسائل خطأ واضحة

### واجهة المستخدم:
- [ ] الألوان متناسقة (بنفسجي/ذهبي)
- [ ] الـ modal يفتح ويغلق بشكل صحيح
- [ ] الـ loading spinner يظهر عند الحفظ
- [ ] الـ notifications تظهر وتختفي تلقائياً
- [ ] الفلترة تعمل بشكل صحيح
- [ ] الـ responsive يعمل على الموبايل

---

## 🐛 المشاكل المحتملة وحلولها

### المشكلة 1: "API endpoint not found"
**الحل**: تأكد من تشغيل الـ server على port 5000

### المشكلة 2: "Specialty not found"
**الحل**: قم بتشغيل seed data:
```bash
cd server
node seed-data.js
```

### المشكلة 3: "No courses available"
**الحل**: أضف مواد من صفحة Courses Management أولاً

### المشكلة 4: المواد لا تظهر بعد الفلترة
**الحل**: تأكد من أن المواد مرتبطة بالتخصص الصحيح في قاعدة البيانات

### المشكلة 5: "academic_year_id is required"
**الحل**: تم إصلاحها! الكود الجديد يجلب academic_year_id و semester_id من بيانات المادة

---

## 📊 النتائج المتوقعة

بعد إكمال جميع الخطوات بنجاح، يجب أن يكون لديك:

1. ✅ دكتور جديد في النظام
2. ✅ 3-5 مواد معينة للدكتور
3. ✅ البيانات محفوظة في قاعدة البيانات
4. ✅ جميع الوظائف تعمل بشكل صحيح
5. ✅ رسائل الخطأ واضحة ومفيدة
6. ✅ الواجهة سهلة الاستخدام

---

## 🎉 الخلاصة

إذا نجحت جميع الاختبارات، فإن نظام إدارة الدكاترة يعمل بشكل مثالي! 🚀

**ملاحظة**: احفظ بيانات الدخول للدكتور الجديد لاختبار صفحة Professor Dashboard لاحقاً.
